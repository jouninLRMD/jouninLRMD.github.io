# The updater deliberately uses only the Python standard library so it runs
# on any vanilla Python 3.10+ without extra dependencies. This file exists as
# an explicit marker and a place to add future dependencies if needed.
